# Portal Client (Student Application)

This is the student-facing application for the Secure Offline Examination System.

## Deployment on Vercel
1. Select the `portal-client` directory as the root.
2. Set the `VITE_API_URL` environment variable to your backend API URL.
3. The framework preset is Vite.
